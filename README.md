The code is in SRC path

This Spark & Scala project implements five key data workflows: it processes student records by filtering high grades and sorting names, labels ages as even or odd with a UDF, and joins student and grade tables to compute average scores. It also counts word occurrences in an RDD and calculates total sales revenue per product by multiplying quantity by unit price and summing the results.
